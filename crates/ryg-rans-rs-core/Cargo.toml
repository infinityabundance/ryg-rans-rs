[package]
name = "ryg-rans-rs-core"
version = "0.5.0"
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
description = "Deterministic algorithmic core of rANS entropy coding - no_std, no_unsafe"

[features]
default = []
alloc = []
std = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(kani)"] }
