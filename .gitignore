# Generated by Cargo
/target/

# Rust incremental artifacts
**/*.rs.bk

# Editor files
*.swp
*.swo
*~
.DS_Store
.vscode/
.idea/
*.iml

# Build artifacts (Phase L.15 repository-cleanliness gate).
# Anchored to the build tree: evidence stored intentionally under
# evidence/ (including raw Criterion trees) MUST be tracked in git and is
# never ignored (see evidence/phase-l/comparative/criterion/).
*.o
*.obj
*.a
*.so
*.dylib
*.dll
*.exe
*.rlib
*.rmeta
*.profraw
*.profdata
/target/criterion/

# Oracle binaries
oracle/adapter/rans_trace
oracle/adapter/*.o
oracle/adapter/*.so

# Reports are stored in the Docker root, not in source
reports/

# Evidence staging area (final evidence is tracked under evidence/)
evidence.staging/
